[pytest]
markers =
    network: tests requiring external network access
asyncio_mode = auto

# Basic options (coverage flags moved to CI pipeline to avoid local plugin dependency errors)
addopts = -q
